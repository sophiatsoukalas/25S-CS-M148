# Project 2

This project performs KNN, logistic regression, and SVM  for binary classification on the UCI Heart Disease dataset. The models are also compared across a hyperparameter set to select the best estimator given 3-fold CV.